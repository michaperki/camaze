// Provider metadata and exact AA configurations reviewed together. See INSIGHTS.md.
// Dateless canonical IDs are explicit entries; never strip dates or guess aliases.
// Candidates are selected from all same-provider entries, not fixed migration pairs.
module.exports = require('./catalog.json');
